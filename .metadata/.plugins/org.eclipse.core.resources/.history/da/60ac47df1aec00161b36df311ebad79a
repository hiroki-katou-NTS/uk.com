package nts.uk.ctx.basis.app.command.position.hoatt;

import javax.ejb.Stateless;
import javax.inject.Inject;

import nts.arc.layer.app.command.CommandHandler;
import nts.arc.layer.app.command.CommandHandlerContext;
import nts.uk.ctx.basic.dom.organization.position.hoatt.CmnmtJobTitleRepository;
import nts.uk.ctx.basic.dom.organization.position.hoatt.Position;

@Stateless
public class DeletePositionCommandHandler extends CommandHandler<DeletePositionCommand>{

	@Inject
	CmnmtJobTitleRepository positionRepo;
	
	@Override
	protected void handle(CommandHandlerContext<DeletePositionCommand> context) {
		
		//Position position = context.getCommand().toDomain();
		
		//position.validate();
		
		//this.positionRepo.remove(position);
		
		
	}

}
