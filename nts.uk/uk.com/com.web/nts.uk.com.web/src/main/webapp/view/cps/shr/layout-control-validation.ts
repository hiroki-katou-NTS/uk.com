module nts.layout {
    export const validate = {
        constructor() {
            let self = this;
        }
    }

    export const constraint = {
        combobox: (subscribeCode: string, targetCode: string) => {

        }
    }

    const fetch = {
        combobox: (baseDate: string, itemCode: string) => {

        }
    }
} 